# Flutter/Dart
.dart_tool/
.packages
.pub/
build/
*.iml

# Android
android/.gradle/
android/app/build/
android/local.properties
android/key.properties
*.jks
*.keystore

# PRoot binaries (fetched by script, not committed)
android/app/src/main/jniLibs/

# Patched plugin copies are committed so every machine uses the same sources.
# Refresh them with: dart run ../scripts/sync_local_plugins.dart

# Local ADB QA captures
adb-openclaw-*.png
adb-openclaw-*.xml

# IDE
.idea/
.vscode/
*.swp

nul
.salive
